const list = document.querySelector('section.comment-list')
const renderContent = "<%= j(render 'pages/comment', comment: @comment) %>"
list.innerHTML = renderContent + list.innerHTML

document.querySelector('form.comment_form').reset()
